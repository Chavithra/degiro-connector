[pytest]
addopts = -p no:warnings --strict-markers
markers = 
    core: core features
    quotecast: everything related to quotecast
    network: marks test cases that make external web requests to data providers
    trading: everything related to trading
    work_in_progress: work in progress
